#include <stdio.h>

int main(void) 
{
  int number[10][10];
  for(int i = 0 ; i < 10 ; i++)
  {
    for(int j = 0 ; j < 10 ; j++)
      {
        number[i][j] = (i + 1)*(j + 1);
        printf("%d\t",number[i][j]);
      }
      printf("\n");
  }
  return 0;
}